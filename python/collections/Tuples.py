 # Create a tuple
tuples = ("apple", "banana", "cherry")

# Print the tuple
print(tuples)

# Access elements of the tuple
first_fruit = tuples[0]
print("First fruit:", first_fruit)

# Attempt to modify an element of the tuple (this will result in an error)
# t[0] = "orange"

# Get the size of the tuple
size = len(tuples)
print("Size of tuple:", size)

# Iterate over the tuple
for fruit in tuples:
    print(fruit)
